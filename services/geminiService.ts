
import { MonthlyStats } from "../types";

// Serviço de IA Desativado conforme solicitação do usuário.
// Funções mantidas como stubs caso ainda existam referências legadas, mas não executam lógica.

export const generateMonthlyAnalysis = async (
  stats: MonthlyStats,
  workerName: string,
  monthName: string
): Promise<string> => {
  return "";
};

export const generateBossMessage = async (
  stats: MonthlyStats,
  workerName: string,
  employerName: string,
  monthName: string
): Promise<string> => {
    return "";
}
